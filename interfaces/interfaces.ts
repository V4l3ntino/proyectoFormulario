export interface Person {
    id: string,
    nombre: string,
    apellido: string,
    sexo: string,
    experiencia: number;
}

export interface Formulario {
    creador : string,
    created_at: string,
    operario: string
}